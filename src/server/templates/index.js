import type from './type';
import wait from './wait';
import click from './click';
import swipe from './swipe';
import scrollUntil from './scroll_until';
import screenshot from './screenshot';

const template = {
  type,
  wait,
  click,
  swipe,
  scrollUntil,
  screenshot,
};

export default template;